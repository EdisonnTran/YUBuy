generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model UserTest {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
